<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(LifeCrm.Web.Components.Layout.MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(LifeCrm.Web.Components.Layout.MainLayout)">
            <p role="alert">Sorry, this address doesn't exist.</p>
        </LayoutView>
    </NotFound>
</Router>
