@* src/LifeCrm.Web/Layout/MainLayout.razor
   UPDATED SECTION ONLY — replace the <nav class="lc-sidebar__nav"> block
   in the previously delivered MainLayout.razor with this version.
   Only the nav content changes; everything else stays identical. *@

@* ── Paste this block to REPLACE the existing <nav class="lc-sidebar__nav"> ── *@


@* ── Also update the MOBILE bottom nav ChildContent section ── *@
@* Replace the existing <nav class="lc-bottom-nav"> with: *@



@* ── And update the mobile drawer nav section ── *@

